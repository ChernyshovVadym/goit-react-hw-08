import DocumentTitle from "../../DocumentTitle";
import LoginForm from "../../components/LoginForm/LoginForm";

// import css from "./LoginPage.module";

const LoginPage = () => {
  return (
    <div>
      <DocumentTitle>Login</DocumentTitle>
      <LoginForm />
    </div>
  );
};

export default LoginPage;
